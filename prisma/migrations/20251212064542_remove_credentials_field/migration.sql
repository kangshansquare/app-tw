/*
  Warnings:

  - You are about to drop the column `region_id` on the `Credentials` table. All the data in the column will be lost.

*/
-- AlterTable
ALTER TABLE `Credentials` DROP COLUMN `region_id`;
